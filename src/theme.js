// ─────────────────────────────────────────────────────────────────────────────
// GLOBAL THEME — Change the accent color here to update the ENTIRE app at once
// ─────────────────────────────────────────────────────────────────────────────

export const ACCENT = "#f97316"; // orange-500  ← change this hex to retheme

// Derived opacity variants (used in inline styles & JS logic)
export const ACCENT_10  = `${ACCENT}1a`;
export const ACCENT_15  = `${ACCENT}26`;
export const ACCENT_20  = `${ACCENT}33`;
export const ACCENT_30  = `${ACCENT}4d`;
export const ACCENT_50  = `${ACCENT}80`;
export const ACCENT_RGB = "249, 115, 22";   // RGB of orange-500 for rgba() use
